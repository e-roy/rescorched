{
  "name": "@scorched/sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure deterministic Scorched Earth simulation. No DOM, no platform APIs, no ambient randomness.",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@scorched/config": "workspace:*",
    "fast-check": "4.9.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
