{
  "name": "@scorched/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phaser 3 renderer + input. No game rules live here.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"@scorched/client: covered by e2e (see /e2e)\""
  },
  "dependencies": {
    "@scorched/protocol": "workspace:*",
    "@scorched/sim": "workspace:*",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@scorched/config": "workspace:*",
    "typescript": "5.9.3",
    "vite": "8.1.5"
  }
}
